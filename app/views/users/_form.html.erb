<%= semantic_form_for @user do |f| %>
  <%= f.semantic_errors %>
  <%= f.inputs do %>
    <%= f.input :twitter, :hint => "Enter the user's twitter handle" %>
  <% end %>
  <%= f.buttons do %>
    <%= f.commit_button :button_html => {:class => "btn-primary"} %>
  <% end %>
<% end %>